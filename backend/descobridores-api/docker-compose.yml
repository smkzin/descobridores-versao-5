services:
  postgres:
    container_name: container-descobridores
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: descobridores
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432